# Tech Radar static proto

- `corepack enable`
- `pnpm i`
- `cp radar.example.csv radar.csv`
- `node parser.mjs`
- `pnpm run start`

If the browser doesn't open automatically when you run the last command, surf to http://localhost:3000/
